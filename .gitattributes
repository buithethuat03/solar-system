# Normalize line endings: store text as LF in the repo, check out native on each OS.
* text=auto

# Treat binary assets correctly (no line-ending conversion, no diff noise).
*.jpg   binary
*.jpeg  binary
*.png   binary
*.glb   binary
*.zip   binary
